# Changelog

## [Unreleased]

### Added

- Added strict JSONL Pi RPC bridge, event normalization, and deterministic fake runtime.
- Added Pi session switching, project trust flags, OpenAI-compatible model materialization, global prompts, and skill directory loading.
- Added bounded automatic sidecar recovery with session restoration and exponential backoff.
