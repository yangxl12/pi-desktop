# Pi Desktop MCP

The desktop MCP client supports MCP JSON-RPC over STDIO and HTTP. It keeps
credentials in the host process, applies project trust/consent before tools
run, enforces timeouts and output limits, and exposes stable namespaced tools.
